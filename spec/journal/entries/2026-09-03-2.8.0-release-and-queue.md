# 2026-09-03 — 2.8.0 릴리즈와 PR 큐(#178–#193) 결과

- **범위:** v2.7.0 이후 develop에 PR 14개. 슬레이트(#137/#146–#152/#156) 3건 + amazon의 grounding 스택 4건 + heal 판정
  1건 + advisory 2건 + 감사 1건 + 문서 1건. 릴리즈 PR = develop→main, 2026-09-03.
- **진행 방식:** 처음엔 7개가 한꺼번에 열렸고(4개가 한 브랜치 위 스택), 큐 전체를 병렬 심층 리뷰 6건으로 감사한 뒤
  **PR 하나씩** 코멘트→수정→실행 검증→머지로 갔다. 모든 코멘트는 실행으로 재현한 사실만 담았고, 상대가 돌려볼 수 있는
  형태(함수·입력·출력, 픽스처 HTML)로 썼다. 승인은 draft 해제 후 마지막 헤드를 다시 돌린 뒤에만.
- **결정·결과(핵심):**
  - #178: 쿼리를 통째로 버리면 쿼리 디스패치 API가 develop보다 나빠져 #179를 합류시킴. 넓어진 접두가 제안보다 많이
    매칭하면 드랍하는 가드 추가(메서드 인지). 읽기 전용 드릴인의 조용한 통과를 이걸로 닫음.
  - #183: `isDigitDenseToken` 흩어진 숫자 런 2→3 — `step2Of3`·`Q1Report2026` 같은 라우트 이름이 id로 읽혀
    `sameEndpointShape`까지 같은 엔드포인트로 판정하던 것. 임계값은 한 글자 여유, 코퍼스에 사유와 함께 기록.
  - #182: 로케일/마운트 접두 아래 와일드카드(`shop.co/en/*`)를 에러 페이지가 만족 → boundaryMatch가 host 제외 전부
    와일드카드면 거부, `localePrefixes`를 `markVacuous`까지 배선, 와일드카드 leaf는 페이지가 아님, `Scenario.wildcards`
    마커(기존 스킬의 리터럴 `*` 보호). `vacuousBecause: "no-destination"`로 사유 분리.
  - #184: heal 우회·워터마크 레이스·Amplitude 무장을 실측 → **advisory로 착륙**, 무장은 same-site(접미 매칭, PSL 불필요),
    플래그에 `METHOD url`, 벤치(#169) 뒤 fail-closed. 실수로 지운 #178 테스트 6개 복구.
  - #186/#189(우리 몫): 판정 마무리 함수 하나. 리뷰 2회(amazon)에서 blocker 2(잘린 heal이 cli `--freeze`로 파일에 써짐,
    `maxSteps` 미전달로 영구 red) → `runScenario`가 검증 안 된 재발견을 아예 돌려주지 않음. amazon 제안 `!judged.passed`는
    **실측으로 틀림**(가드 트립이 올바른 수리를 버림) → `goalFailures`로 양쪽(트리거·반환) 판정. 사전 병렬 검증 4건에서
    소비자 경로·오탐·표면·회귀(재생 경로 5000 랜덤 입력 바이트 동일) 확인 후 푸시.
  - #180: 트리거를 트레이스(드랍 발생)에서 시나리오(살아있는 증명 유무)로. `expect`는 이름만 부르고 세지 않음.
  - #181: 프로브를 reachable/occluded/unknown 3상태로, 양성 폐색만 좁힘. 실제 크롬 픽스처(#185)가 CI에 들어옴.
  - #193(amazon 루프 산출): 9k줄 단일 PR을 논리 단위로 재구성 요청 → 7 픽스 각 커밋 + 모듈별 커버리지, 루프 파일 제외로 재구성됨.
- **배운 것(규칙 후보로 승격, state.md):** 스택 PR은 CI 0회 / 리뷰 주장은 재현 가능하게 / heal은 목표 단언으로 /
  루프 PR은 쪼갠 뒤 리뷰. 그리고 제 실수 둘 — PR 상태를 코멘트 글로 추정(isDraft 미확인), 계획 질문에 릴리즈 준비로 반응.
- **state 변화:** 2.8.0 배포. #172·#176 종결. 2.9.0 = 슬레이트 잔여(#171/#173/#174/#175/#177) + 소비자 2차 제보 신규 4
  + #193 조각. #184 fail-closed 전환·#169 벤치는 그대로 대기.
