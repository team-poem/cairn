# 2026-07-06 — 이슈 슬레이트 #96–#105 등록 + 2.4.0 방향

- **브랜치:** `develop` (코드 변경 없음 — 분석·이슈 등록·저널 갱신)
- **계기:** 외부 관점 평가(Slack engineering agentic-testing + 에이전트 테스트 비용 구조 분석)로 엔진 포지션 점검 + 코어 전소스(~3.5k라인) 재리뷰 + 익스텐션 next-steps 리포트 교차 대조. 기존 오픈 이슈 19건과 겹침 전수 확인 후 신규만 등록.
- **평가 결론:** 두 글이 트레이드오프로 받아들인 "에이전트 테스트는 비싸고($15–30/run) 비결정적이라 CI 불가"를 cairn은 상환 구조(discover 1회 → replay $0)로 정면 해소 — 구조적 막힘 없음. 남은 갭은 측정·노출(비용 리포팅), 턴 수 절감(look-ahead), 표본 확대(벤치).
- **코드 리뷰 발견(버그):**
  - **#96** — `assignStepExpects`가 풀 URL로 변화를 감지하고 host+path로 얼림 → 쿼리/해시만 바뀌는 이동(페이지네이션·SPA 해시)이면 expect가 이동 *전* 페이지에서 이미 참 → replay 사전체크가 스텝을 조용히 skip. #86·#87과 "expect URL 가족".
  - **#97** — `parseNetwork`가 숫자 status만 매칭, MCP는 in-flight를 `[pending]`으로 렌더 → 레퍼런스 드라이버에서 #81의 mutation 대기가 dead code. 느린 마지막 submit이 expect 없이 얼려짐.
  - **#98** — outcome-heal이 close된 드라이버로 재발견(암묵 재연결 의존) + 재발견 후 미close(임베드 호스트 누수) + `close()` 부분 리셋(initialUrl·snapshotCache 잔존). **#88(재연결 fatal화)과 동반 설계 필수** — 단독 구현 시 상호 파괴.
- **코드 리뷰 발견(정리):** **#99** — 프롬프트가 제안 허용한 `no-console-errors`를 deriveAssertions가 무언 드랍(vocabulary 드리프트). **#105** — request-status 단언에 method 파리티(#94 잔여).
- **방향성 이슈:** **#100** LLM usage 리포팅(측정은 LlmClient seam이 소유 — 호스트 클라이언트도 꽂히게) · **#101** look-ahead 배치 플래닝(익스텐션 수요 확정, 캐싱과 곱 효과) · **#102** freeze-less 탐색 모드(엔진은 능력만) · **#103** 벤치 복잡도축+대수(replay 반복은 $0인 구조 이점) · **#104** vision 판정(결정적 스크린샷 diff 게이트, 발산 시에만 opt-in vision — surgical-heal 패턴 반복).
- **기존 이슈 정리:** **#94 close**(제보는 2.2.2 기준, `459315c`/PR #70이 이미 해소 — 잔여는 #105로) · **#76 코멘트**(outcome-heal이 `actions`도 미상속) · **#89 코멘트**(followNewTab 동사 커버리지).
- **착수 묶음(기존 이슈 포함, 같은 파일 = 한 브랜치):** A=#96·#86·#87(expect URL 가족) · B=#97·#85·#91(chrome.ts 정비, +#89 합류 가능) · C=#98·#78·#76(outcome-heal, #88 동반) · D=#93·#99(discover LLM 표면, #61 재트리아지) · E=#90·#105(critic 소품) · F=#2·#13(타입 위생). A·B·C 파일 비겹침 → 병렬 가능.
- **릴리스 방향:** 다음 = **2.4.0(minor, breaking 0)**. 스코프 제안 = 신뢰성 버그(#85–#87·#90–#91·#96–#98) + 이월(#76·#78·#79) + 잔손질(#95·#99·#105) + 헤드라인 #100. need-to-discussion(#92·#101·#102·#104)은 합의 후 편입, #103은 릴리스 무관. 도그푸딩이 막히면 #96·#97만 2.3.1 패치 선행 옵션.
- **꼬리물기 진단(회귀 체인 브리핑 검증 후 확정):** 원인 2개 — ⓐ 앱 형질의 보편 휴리스틱화(#56→#86·#87; seam 주입으로 넘어온 benign/ActionPolicy는 회귀 0) ⓑ 테스트 더블 가정 ≠ 실물 어댑터(#81→#97). 브리핑 주장 중 "#68 불완전 수정"은 기각(PR #70이 critic까지 수정, #94는 2.2.2 버전 랙), "#96=#56 계보"는 정정(#96은 #81 산물 `8f696bc`), "amazon 로컬 브랜치 구현 완료"는 근거 없음(원격 무흔적·시간 모순 — 착수 전 구두 확인으로 대체).
- **설계 장치 3(완료 조건 편입, state 규칙 후보 등재):** ① 형질/사실 검문(형질이면 seam+주입 — #86은 로케일 리스트 정교화 대신 2단 매칭+주입 옵션으로) ② URL 매칭 반례 코퍼스(묶음 A 머지 조건) ③ 실물 픽스처 규칙(승격 확정, #97·#93 적용).
- **state 변화:** 2.3.0 배포 완료 반영 · "2.3.0 계획" 섹션 완료 처리 · "2.4.0 계획 — 이슈 슬레이트" 신설(후속 후보 중 look-ahead·멀티모달은 #101·#104로 승격됨을 표기) · 규칙 후보에 검문/코퍼스 추가 + 픽스처 규칙 승격 · 담당 분배·설계 장치 등재. 최종 실행 문서 = 데스크탑 `cairn-2.4.0-final-plan.html`(팀 공유용, 정본은 state).
